// :id
// second forRoot

// 1: router basic configuration
// RouterModule.forRoot(routes);
// routes = {path: component: fullMath redirectTo: canActivate canDeactivate canLoad resolve outlet, children}
// router-outlet
// 2: navigation in lazy loaded module (loadChildren and create forChild with '' route)
// 3: how to navigate from view  and from component logic
// 4: What is resolver? if we go one level deeper from resolver will it resolve again?
// 5: what can be if we make second .forRoot in the LL module

//* explain the parts of url: users/1/orders/12(sidenav: main)?line=99#myHref
//* all pages with navbar, one page without navbar - how to configure this case

// routerEvents
// get data from params, queryParams
// params vs queryParams