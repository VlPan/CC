// Basic tests configuration [TestBed, fixture, debugElement, By.css, By.directive]
// What is jasmine, what is Karma?
// How not to mock all components with its dependenciec [CUSTOM_ELEMENTS_SCHEMA vs NO_ERRORS_SCHEMA] vs create Mock components
// how to detect changes [fixture.detectChanges(), get the new value from template, debugEl.query(By.css).nativeElement , etc]
// fakeAsync + tick combo [limitation on real calls]
// async + done
// angular async + fixture.whenStable
// async (done) + fixture.whenStable
// configure services while Unit testing provide: useValue: useFabric: useExisting: etc
// testing inputs / outputs [el.triggerEventHandler('click', null)]
// testing directive [by creating simple component]
// testing observables [simple scenario with two stream with zip and update encapsulated value]
// tesitng routing  [RouterTestingModule, intialNavigation, router.navigate + tick + expect(loction.path).toBe]
